# Project Memory — Overview

> Single source of truth map for this project's memory bank. Read this first,
> then open only the file you need. The bank describes what the code does and how
> we build it — in prose, tables, and signatures-as-text, never pasted source code.

## How to use

Read the transient state first — `progress.md` (what works / what's left) and
`active-context.md` (what is being worked on right now) — then the durable docs
in this order to understand or rebuild the system bottom-up:

- `architecture/` — module/file layout, layering, dependencies, build + config + env. Read first: the shape to scaffold.
- `data/`         — data model, schema, state, formats, invariants, constants. Read next: the shapes the code moves.
- `interface/`    — public contracts: API, CLI, signatures, events, config keys. Read to know what each unit must expose.
- `behavior/`     — what each unit must do: logic, algorithms, flow, edge cases. Read to fill in the logic.
- `rule/`         — conventions, procedures, how-to ("how we do X"). Read to follow project practice.

Trust each durable statement by its confidence marker: 🟢 `confirmed` (cited from
code), 🟡 `inferred` (deduced — verify against source), 🔴 `gap` (unresolved —
needs a human before you rely on it).

## Map

<!-- BEGIN:MAP (auto-generated by memory_docs.py overview - do not edit by hand) -->
| Category | File | Purpose | Status | Keywords |
|----------|------|---------|--------|----------|
| architecture | [architecture/delivery-phases.md](architecture/delivery-phases.md) | The finished product news-radar aims at, and the phase-by-phase task breakdown that gets there. | active | roadmap, phases, P0, P1, P2, P3, P4, P5, P6, scope, milestones, definition of done |
| architecture | [architecture/module-layout.md](architecture/module-layout.md) | The directory tree news-radar is built as, its layering rules, and every dependency it is allowed to take. | draft | tree, layout, layering, dependencies, pyyaml, feedparser, python 3.12, src/news_radar, scripts, docker |
| architecture | [architecture/deployment-homelab.md](architecture/deployment-homelab.md) | How news-radar runs on the homelab and how https://news.dtbao.org reaches the outside world. | draft | news.dtbao.org, homelab, docker compose, caddy, cloudflare tunnel, schedule, volumes, restart policy |
| data | [data/news-sources.md](data/news-sources.md) | Every source news-radar pulls from - the fixed feed list, the keyword-driven search URL templates, and what each one returns. | draft | sources, feeds, RSS, Atom, hnrss, lobste.rs, hackaday, lwn, reddit, vnexpress, genk, tinhte, google news rss, hn algolia, search url, user-agent |
| data | [data/news-item.md](data/news-item.md) | The shape every story is normalised into, how duplicates collapse, and what lands on disk under output/. | draft | NewsItem, dedup key, canonical url, sqlite schema, news.db, output layout, index.html, seen set, snapshot |
| adr | [adr/adr-0001-clean-room-from-trendradar.md](adr/adr-0001-clean-room-from-trendradar.md) | Why news-radar is written from scratch instead of forking TrendRadar, and what that forbids. | active | ADR-0001, clean-room, TrendRadar, GPL-3.0, license, fork, copyleft, reference |
<!-- END:MAP -->

## Conventions

Durable docs follow semantic `kebab-case.md` naming inside their category folder,
with frontmatter (`title`, `category`, `purpose`, `status`, `updated`, `source`,
`confidence`, `keywords`, optional `order`). Look a doc up by theme in `Purpose`
or by exact name in `Keywords`. Reading order comes from the category order above
then the `order` field, not the file name. One concept per file, under ~300
lines, front-load a one-line summary, cite the source path (with `:line` where it
helps), link instead of repeating. The two state files carry only `title` +
`updated` and are exempt from category naming. See the `skill-memory-ai` reference
`memory-doc-rules.md` for the full rules.
