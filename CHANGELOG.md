# Changelog

Everything notable in this project, newest first.

Sections are written by `python scripts/release.py <version>` from the
conventional-commit subjects since the previous tag, and the CI release workflow
publishes the section for a tag as that release's notes. Edit a section by hand
only to fix wording - never to add a version heading, or the two will disagree.
